en:
  graphql:
    enums:
      task_recover_location:
        desc: Recover Location for task enum
        values:
          anytime: Anytime
          today: Today
      area_type_enum:
        desc: Area type enum
        values:
          note_area: Note area
          task_area: Task area
      project_type_enum:
        desc: Project type enum
        values:
          task_project: Task project
          note_project: Note project
      direction_enum:
        desc: Order direction enum
        values:
          asc: Ascending ordering
          desc: Descending ordering
      task_filter:
        desc: Task filter enum
        values:
          anytime: anytime
          logbook: logbook
          today: today
          trash: trash
          upcoming: upcoming
      task_note_property_sort:
        desc: Task and note enum for sorting
        values:
          created_at: created_at
          name: name
          updated_at: updated_at
