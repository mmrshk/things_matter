en:
  graphql:
    enums:
      area_type_enum:
        desc: Area type enum
        values:
          note_area: Note area
          task_area: Task area
      project_type_enum:
        desc: Project type enum
        values:
          task_project: Task project
          note_project: Note project
      direction_enum:
        desc: Order direction enum
        values:
          asc: Ascending ordering
          desc: Descending ordering
      movie_sort_enum:
        desc: Order field enum
        values:
          title: Order by title
