en:
  graphql:
    mutations:
      desc: The schema’s entry-point for mutations.
      common:
        fields:
          id: ID
          uuid: UUID
      user:
        area:
          create:
            desc: Add Area to current user's areas
          update:
            desc: Update current user's Area
          delete:
            desc: Delete current user's Area
        project:
          create: Add Project to current user's projects
        sign_up:
          desc: Sign up a user
        sign_out:
          desc: Sign out a user
          fields:
            completed: will return true if the user log out
        sign_in:
          desc: Sign in a user
        list:
          create:
            desc: Create new list for User
          add_item:
            desc: Add the new movie to the current user's list
          remove_item:
            desc: Remove the movie from the current user's list
          delete:
            desc: Remove the list from user
        movie:
          add_favorite:
            desc: Add movie to favorite
          add_watchlist:
            desc: Add movie to watchlist
      auth:
        refresh_token:
          desc: Updating current access token with refresh token
