en:
  errors:
    # dry-rb predicates
    or: "or"
    array?: "must be an array"
    empty?: "must be empty"
    excludes?: "must not include %{value}"
    excluded_from?:
      arg:
        default: "must not be one of: %{list}"
        range: "must not be one of: %{list_left} - %{list_right}"
    exclusion?: "must not be one of: %{list}"
    eql?: "must be equal to %{left}"
    unique?: "has already been taken"
    not_eql?: "must not be equal to %{left}"

    filled?: "Can’t be blank"

    format?: "is in invalid format"

    number?: "must be a number"

    odd?: "must be odd"

    even?: "must be even"

    gt?: "must be greater than %{num}"

    gteq?: "must be greater than or equal to %{num}"

    hash?: "must be a hash"

    included_in?:
      arg:
        default: "must be one of: %{list}"
        range: "must be one of: %{list_left} - %{list_right}"
    inclusion?: "must be one of: %{list}"

    includes?: "must include %{value}"

    invitation_token?: "token not found"

    bool?: "must be boolean"

    true?: "must be true"

    false?: "must be false"

    int?: "must be an integer"

    float?: "must be a float"

    decimal?: "must be a decimal"

    date?: "must be a date"

    date_time?: "must be a date time"

    time?: "must be a time"

    key?: "is missing"

    attr?: "is missing"

    lt?: "must be less than %{num}"

    lteq?: "must be less than or equal to %{num}"

    max_size?: "size cannot be greater than %{num}"

    min_size?: "size cannot be less than %{num}"

    none?: "cannot be defined"

    str?: "must be a string"

    type?: "must be %{type}"

    uuid_v4?: uuid not valid

    size?:
      arg:
        default: "size must be %{size}"
        range: "size must be within %{size_left} - %{size_right}"

      value:
        string:
          arg:
            default: "length must be %{size}"
            range: "length must be within %{size_left} - %{size_right}"

    list_exists?: "You are already have list with that name"
    movie_in_list?: "This movie already in list"
    movie_exists?: "Such movie does not exists"
    movie_in_favorite?: "Movie already in favorites list"
    movie_in_watchlist?: "Movie already in watchlist list"

    # custom predicates
    project_existence?: project not exist
    valid_deadline?: "deadline time is incorrect"
    area_belongs_to_user?: "area not belongs to user"
    area_existence?: area doesn't exist

    unique_user_account_email?:  "That email address is being used by another account"
